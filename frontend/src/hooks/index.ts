/**
 * Hooks barrel export
 *
 * Central export for all custom hooks.
 */

export { usePermission, type Permission } from './usePermission';
export { useFeatureFlag, type FeatureFlag } from './useFeatureFlag';
export {
    useDataFetch,
    type UseDataFetchOptions,
    type UseDataFetchResult,
} from './useDataFetch';
